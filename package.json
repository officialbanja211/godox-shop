{
  "name": "godox-shop-fullstack",
  "version": "2.0.0",
  "private": true,
  "description": "GODOX-SHOP frontend + hardened backend",
  "main": "server.js",
  "scripts": {
    "start": "node --require ./turso-compat-hook.cjs server.js",
    "dev": "node --watch server.js",
    "build": "node scripts/build-netlify-config.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@libsql/client": "^0.18.0"
  }
}
